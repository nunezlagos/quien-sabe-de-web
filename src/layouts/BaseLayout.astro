---
const { title = "QuiénSabe - Conecta con oficios de tu barrio", description, image } = Astro.props;
import '../styles/base.css';
import RoleSwitcher from '../components/navbar/RoleSwitcher.astro';

const currentUser = Astro.locals.user;
const isLoggedIn = Boolean(currentUser);
const currentPath = Astro.url.pathname;
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/favicon.svg">
    <title>{title}</title>
    <meta name="description" content="Encuentra gasfíteres, electricistas y más oficios en tu comunidad.">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.min.js" is:inline></script>
    <meta property="og:title" content={title} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="QuiénSabe" />
    {description ? <meta property="og:description" content={description} /> : null}
    {image ? <meta property="og:image" content={image} /> : null}
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    {description ? <meta name="twitter:description" content={description} /> : null}
    {image ? <meta name="twitter:image" content={image} /> : null}
</head>
<body class="page-body">

    <a href="#main" class="skip-link">Saltar al contenido</a>

    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-brand">
                <div class="nav-logo">
                    <img src="/favicon.svg" alt="" aria-hidden="true" width="40" height="40" />
                </div>
                <span class="nav-brand-text">
                    Quién<span class="brand-accent">Sabe</span>
                </span>
            </a>

            <div class="nav-actions">
                {isLoggedIn && currentUser ? (
                    <>
                        <RoleSwitcher
                            userRoles={currentUser.roles || [currentUser.role]}
                            currentRole={currentUser.activeRole || currentUser.role}
                            currentPath={currentPath}
                        />
                        <a href={currentUser.role === 'provider' ? '/dashboard-prestador' : '/dashboard'} class="nav-btn nav-btn-user">
                            <i class="ri-user-line" aria-hidden="true"></i>
                            <span class="nav-user-name">{currentUser.name.split(' ')[0]}</span>
                        </a>
                        <form method="POST" action="/api/v1/auth/cerrar-sesion" class="nav-logout-form js-logout-form" id="form-cerrar-sesion">
                            <button type="submit" class="nav-btn nav-btn-logout" aria-label="Cerrar sesión">
                                <i class="ri-logout-box-r-line" aria-hidden="true"></i>
                            </button>
                        </form>
                    </>
                ) : (
                    <>
                        <a href="/donar" class="nav-btn nav-btn-donate">
                            <i class="ri-heart-3-fill" aria-hidden="true"></i>
                            <span>Donar</span>
                        </a>
                        <a href="/iniciar-sesion" class="nav-btn nav-btn-outline">
                            Ingresar
                        </a>
                        <a href="/registro" class="nav-btn nav-btn-primary">
                            Crear cuenta
                        </a>
                    </>
                )}
            </div>

            <button
                type="button"
                class="nav-mobile-toggle js-nav-toggle"
                aria-label="Abrir menú"
                aria-expanded="false"
                aria-controls="nav-mobile-panel"
            >
                <i class="ri-menu-line" aria-hidden="true"></i>
            </button>
        </div>

        <!-- Mobile drawer -->
        <div id="nav-mobile-panel" class="nav-mobile-panel" hidden>
            <div class="nav-mobile-links">
                {isLoggedIn && currentUser ? (
                    <>
                        <a href={currentUser.role === 'provider' ? '/dashboard-prestador' : '/dashboard'} class="nav-mobile-link">
                            <i class="ri-user-line" aria-hidden="true"></i>
                            <span>Mi panel</span>
                        </a>
                        <form method="POST" action="/api/v1/auth/cerrar-sesion" class="nav-logout-form js-logout-form">
                            <button type="submit" class="nav-mobile-link nav-mobile-link-logout">
                                <i class="ri-logout-box-r-line" aria-hidden="true"></i>
                                <span>Cerrar sesión</span>
                            </button>
                        </form>
                    </>
                ) : (
                    <>
                        <a href="/donar" class="nav-mobile-link">
                            <i class="ri-heart-3-fill" aria-hidden="true"></i>
                            <span>Donar</span>
                        </a>
                        <a href="/iniciar-sesion" class="nav-mobile-link">
                            <i class="ri-login-box-line" aria-hidden="true"></i>
                            <span>Ingresar</span>
                        </a>
                        <a href="/registro" class="nav-mobile-link nav-mobile-link-primary">
                            <i class="ri-user-add-line" aria-hidden="true"></i>
                            <span>Crear cuenta</span>
                        </a>
                    </>
                )}
            </div>
        </div>
    </nav>

    <main id="main">
        <slot />
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-inner">
            <a href="/" class="footer-brand">
                <img src="/favicon.svg" alt="" aria-hidden="true" width="24" height="24" />
                <span>Quién<span class="footer-brand-accent">Sabe</span></span>
            </a>

            <nav class="footer-nav" aria-label="Enlaces del pie">
                <a href="/" class="footer-link">Buscar</a>
                <a href="/donar" class="footer-link">Donar</a>
                <a href="/terminos" class="footer-link" data-legal-src="/terminos" data-legal-title="Términos y Condiciones">Términos</a>
                <a href="/privacidad" class="footer-link" data-legal-src="/privacidad" data-legal-title="Política de Privacidad">Privacidad</a>
                <a href="/transparency" class="footer-link">Transparencia</a>
            </nav>

            <div class="footer-social">
                <a href="/donar" class="footer-social-link" title="Donar" aria-label="Donar">
                    <i class="ri-heart-3-fill" aria-hidden="true"></i>
                </a>
                <a href="https://github.com/nunezlagos/quien-sabe-de-web" class="footer-social-link" title="GitHub" target="_blank" rel="noopener" aria-label="GitHub">
                    <i class="ri-github-fill" aria-hidden="true"></i>
                </a>
            </div>
        </div>
        <p class="footer-copyright">&copy; 2026 QuiénSabe · Hecho en Chile 🇨🇱</p>
    </footer>

    <!-- Legal Modal -->
    <div id="legal-modal" class="legal-modal hidden" role="dialog" aria-modal="true" aria-labelledby="legal-modal-title" aria-hidden="true">
        <div class="legal-backdrop close-legal"></div>
        <div class="legal-card">
            <header class="legal-modal-head">
                <h2 id="legal-modal-title" class="legal-modal-title">Documento</h2>
                <a data-legal-openlink href="#" class="legal-modal-icon" title="Abrir página completa" aria-label="Abrir página completa">
                    <i class="ri-external-link-line" aria-hidden="true"></i>
                </a>
                <button type="button" class="legal-modal-icon close-legal" aria-label="Cerrar">
                    <i class="ri-close-line" aria-hidden="true"></i>
                </button>
            </header>
            <div id="legal-modal-body" class="legal-modal-body" tabindex="0"></div>
        </div>
    </div>

    <!-- Cookie Consent Modal -->
    <div id="cookie-modal" class="cookie-modal" role="dialog" aria-modal="true" aria-labelledby="cookie-title" aria-describedby="cookie-desc">
        <div class="cookie-backdrop"></div>
        <div class="cookie-card">
            <div class="cookie-icon">
                <i class="ri-cookie-line" aria-hidden="true"></i>
            </div>
            <h2 id="cookie-title" class="cookie-title">Aviso de cookies</h2>
            <p id="cookie-desc" class="cookie-desc">
                Usamos cookies para mejorar tu experiencia. Las técnicas son necesarias para el funcionamiento; las analytics son opcionales y nos ayudan a mejorar.
            </p>
            <div class="cookie-actions">
                <button id="cookie-reject" class="cookie-btn cookie-btn-secondary">Solo necesarias</button>
                <button id="cookie-accept" class="cookie-btn cookie-btn-primary">Aceptar todas</button>
            </div>
            <a href="/terminos" class="cookie-link">Leer política de cookies</a>
        </div>
    </div>

    <script>
        import { initLogout } from '../lib/client/auth/logout';
        import { initLegalModal } from '../lib/client/legal-modal';
        initLogout();
        initLegalModal();

        (function() {
            const toggle = document.querySelector('.js-nav-toggle');
            const panel = document.getElementById('nav-mobile-panel');
            if (!(toggle instanceof HTMLElement) || !panel) return;

            function openPanel() {
                panel.hidden = false;
                requestAnimationFrame(() => panel.classList.add('is-open'));
                toggle.setAttribute('aria-expanded', 'true');
                toggle.setAttribute('aria-label', 'Cerrar menú');
                const icon = toggle.querySelector('i');
                if (icon) { icon.classList.remove('ri-menu-line'); icon.classList.add('ri-close-line'); }
                document.addEventListener('keydown', onKeydown);
                document.addEventListener('click', onClickOutside);
            }

            function closePanel() {
                panel.classList.remove('is-open');
                toggle.setAttribute('aria-expanded', 'false');
                toggle.setAttribute('aria-label', 'Abrir menú');
                const icon = toggle.querySelector('i');
                if (icon) { icon.classList.remove('ri-close-line'); icon.classList.add('ri-menu-line'); }
                document.removeEventListener('keydown', onKeydown);
                document.removeEventListener('click', onClickOutside);
                const onEnd = () => { panel.hidden = true; panel.removeEventListener('transitionend', onEnd); };
                panel.addEventListener('transitionend', onEnd);
            }

            function onKeydown(e) {
                if (e.key === 'Escape') { closePanel(); toggle.focus(); }
            }

            function onClickOutside(e) {
                const t = e.target;
                if (t instanceof Node && !panel.contains(t) && !toggle.contains(t)) closePanel();
            }

            toggle.addEventListener('click', function(e) {
                e.stopPropagation();
                const isOpen = toggle.getAttribute('aria-expanded') === 'true';
                if (isOpen) closePanel(); else openPanel();
            });
        })();

        (function() {
            const modal = document.getElementById('cookie-modal');
            const acceptBtn = document.getElementById('cookie-accept');
            const rejectBtn = document.getElementById('cookie-reject');
            if (!modal || !acceptBtn || !rejectBtn) return;
            const consent = localStorage.getItem('cookie-consent');
            if (consent === 'accepted' || consent === 'rejected') return;
            modal.classList.add('cookie-modal-visible');
            acceptBtn.addEventListener('click', function() {
                localStorage.setItem('cookie-consent', 'accepted');
                modal.classList.remove('cookie-modal-visible');
            });
            rejectBtn.addEventListener('click', function() {
                localStorage.setItem('cookie-consent', 'rejected');
                modal.classList.remove('cookie-modal-visible');
            });
        })();
    </script>
</body>
</html>

<style>
    .page-body {
        background: var(--bg-page);
        color: var(--text-primary);
        font-family: 'Nunito', sans-serif;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
    }

    /* === SKIP LINK === */
    .skip-link {
        position: absolute;
        left: 1rem;
        top: -3rem;
        z-index: 400;
        padding: 0.625rem 1rem;
        background: var(--color-primary);
        color: white;
        font-weight: 700;
        font-size: 0.875rem;
        border-radius: var(--radius-md);
        text-decoration: none;
        transition: top 200ms ease;
    }

    .skip-link:focus-visible {
        top: 1rem;
        outline: 2px solid var(--border-focus);
        outline-offset: 2px;
    }

    /* === NAVBAR === */
    .navbar {
        background: white;
        border-bottom: 1px solid var(--border-default);
        position: sticky;
        top: 0;
        z-index: 100;
        box-shadow: 0 1px 3px rgba(41, 37, 36, 0.04);
        overflow: hidden;
    }

    .nav-container {
        max-width: 1280px;
        margin: 0 auto;
        padding: 0.75rem 1rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 0.75rem;
        width: 100%;
        box-sizing: border-box;
    }

    .nav-brand {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
    }

    .nav-logo {
        width: 2.5rem;
        height: 2.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .nav-logo img {
        width: 100%;
        height: 100%;
        object-fit: contain;
    }

    .nav-brand-text {
        font-size: 1.375rem;
        font-weight: 800;
        color: var(--stone-800);
        letter-spacing: -0.02em;
    }

    .brand-accent {
        color: var(--amber-600);
    }

    .nav-actions {
        display: none;
        align-items: center;
        gap: 0.5rem;
        flex-shrink: 0;
    }

    @media (min-width: 768px) {
        .nav-actions {
            display: flex;
        }
    }

    .nav-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.375rem;
        padding: 0.5rem 1rem;
        min-height: 44px;
        border-radius: var(--radius-lg);
        font-size: 0.875rem;
        font-weight: 700;
        text-decoration: none;
        transition: all 200ms ease;
        cursor: pointer;
        border: none;
    }

    .nav-btn:focus-visible {
        outline: 2px solid var(--border-focus);
        outline-offset: 2px;
    }

    .nav-btn-donate {
        background: var(--green-50);
        color: var(--green-700);
        border: 1px solid var(--green-200);
    }

    .nav-btn-donate:hover {
        background: var(--green-100);
        border-color: var(--green-300);
    }

    .nav-btn-outline {
        background: transparent;
        color: var(--stone-600);
        border: 1px solid var(--border-default);
    }

    .nav-btn-outline:hover {
        background: var(--stone-50);
        color: var(--stone-800);
        border-color: var(--stone-300);
    }

    .nav-btn-primary {
        background: linear-gradient(135deg, var(--orange-500) 0%, var(--orange-600) 100%);
        color: white;
        box-shadow: 0 2px 4px rgba(249, 115, 22, 0.3);
    }

    .nav-btn-primary:hover {
        transform: translateY(-1px);
        box-shadow: 0 4px 8px rgba(249, 115, 22, 0.4);
    }

    .nav-btn-user {
        background: var(--stone-100);
        color: var(--stone-700);
        border: 1px solid var(--stone-200);
    }

    .nav-btn-user:hover {
        background: var(--stone-200);
    }

    .nav-user-name {
        display: none;
    }

    @media (min-width: 640px) {
        .nav-user-name {
            display: inline;
        }
    }

    .nav-btn-logout {
        background: transparent;
        color: var(--stone-500);
        padding: 0.5rem;
        min-width: 44px;
        border: none;
    }

    .nav-btn-logout:hover {
        color: var(--color-red-700);
        background: var(--color-red-50);
    }

    .nav-logout-form {
        margin: 0;
    }

    .nav-mobile-toggle {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 2.5rem;
        height: 2.5rem;
        background: var(--stone-100);
        border: none;
        border-radius: var(--radius-md);
        color: var(--stone-600);
        font-size: 1.25rem;
        cursor: pointer;
        transition: all 200ms ease;
    }

    .nav-mobile-toggle:hover {
        background: var(--stone-200);
        color: var(--stone-800);
    }

    .nav-mobile-toggle:focus-visible {
        outline: 2px solid var(--border-focus);
        outline-offset: 2px;
    }

    @media (min-width: 768px) {
        .nav-mobile-toggle {
            display: none;
        }
    }

    /* === MOBILE DRAWER === */
    .nav-mobile-panel {
        border-top: 1px solid var(--border-default);
        background: white;
        overflow: hidden;
        max-height: 0;
        opacity: 0;
        transition: max-height 250ms ease, opacity 250ms ease;
    }

    .nav-mobile-panel.is-open {
        max-height: 24rem;
        opacity: 1;
    }

    .nav-mobile-links {
        max-width: 1280px;
        margin: 0 auto;
        padding: 0.5rem 1rem 1rem;
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
    }

    .nav-mobile-links .nav-logout-form {
        margin: 0;
    }

    .nav-mobile-link {
        display: flex;
        align-items: center;
        gap: 0.625rem;
        width: 100%;
        min-height: 44px;
        padding: 0.625rem 0.75rem;
        border-radius: var(--radius-lg);
        font-size: 0.9375rem;
        font-weight: 700;
        text-decoration: none;
        color: var(--stone-700);
        background: transparent;
        border: none;
        cursor: pointer;
        text-align: left;
        transition: background 200ms ease, color 200ms ease;
    }

    .nav-mobile-link i {
        font-size: 1.125rem;
    }

    .nav-mobile-link:hover {
        background: var(--stone-100);
        color: var(--stone-900);
    }

    .nav-mobile-link:focus-visible {
        outline: 2px solid var(--border-focus);
        outline-offset: 2px;
    }

    .nav-mobile-link-primary {
        color: var(--orange-600);
    }

    .nav-mobile-link-logout:hover {
        color: var(--color-red-700);
        background: var(--color-red-50);
    }

    @media (min-width: 768px) {
        .nav-mobile-panel {
            display: none;
        }
    }

    /* === FOOTER (minimalista) === */
    .footer {
        background: linear-gradient(180deg, var(--stone-800) 0%, var(--stone-900) 100%);
        color: var(--stone-300);
        margin-top: auto;
        padding: 1.25rem 1rem 1.5rem;
    }

    .footer-inner {
        max-width: 1280px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .footer-brand {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 1.05rem;
        font-weight: 800;
        color: white;
        text-decoration: none;
    }

    .footer-brand-accent {
        color: var(--orange-500);
    }

    .footer-nav {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 0.75rem 1.25rem;
    }

    .footer-link {
        font-size: 0.8125rem;
        color: var(--stone-300);
        text-decoration: none;
        background: none;
        border: none;
        padding: 0;
        font: inherit;
        cursor: pointer;
        transition: color 200ms ease;
    }

    .footer-link:hover {
        color: var(--amber-400);
    }

    .footer-social {
        display: flex;
        gap: 0.5rem;
    }

    .footer-social-link {
        width: 2rem;
        height: 2rem;
        border-radius: var(--radius-full);
        background: var(--stone-800);
        border: 1px solid var(--stone-700);
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--stone-400);
        font-size: 1rem;
        text-decoration: none;
        transition: all 200ms ease;
    }

    .footer-social-link:hover {
        background: var(--amber-600);
        border-color: var(--amber-600);
        color: white;
    }

    .footer-copyright {
        max-width: 1280px;
        margin: 0.75rem auto 0;
        padding-top: 0.75rem;
        border-top: 1px solid var(--stone-800);
        font-size: 0.75rem;
        color: var(--stone-500);
        text-align: center;
    }

    /* === LEGAL MODAL === */
    .legal-modal {
        position: fixed;
        inset: 0;
        z-index: 200;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
    }

    .legal-modal.hidden {
        display: none;
    }

    .legal-backdrop {
        position: absolute;
        inset: 0;
        background: rgba(41, 37, 36, 0.55);
        backdrop-filter: blur(2px);
    }

    .legal-card {
        position: relative;
        z-index: 1;
        width: 100%;
        max-width: 720px;
        max-height: 85vh;
        display: flex;
        flex-direction: column;
        background: white;
        border-radius: var(--radius-2xl);
        box-shadow: var(--shadow-card-hover);
        overflow: hidden;
    }

    .legal-modal-head {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.875rem 1.25rem;
        border-bottom: 1px solid var(--border-default);
    }

    .legal-modal-title {
        flex: 1;
        font-size: 1.05rem;
        font-weight: 800;
        color: var(--text-primary);
    }

    .legal-modal-icon {
        width: 2rem;
        height: 2rem;
        border-radius: var(--radius-lg);
        display: flex;
        align-items: center;
        justify-content: center;
        border: 1px solid var(--border-default);
        background: white;
        color: var(--text-secondary);
        font-size: 1.05rem;
        cursor: pointer;
        text-decoration: none;
        transition: all 200ms ease;
    }

    .legal-modal-icon:hover {
        background: var(--stone-50);
        color: var(--text-primary);
    }

    .legal-modal-body {
        overflow-y: auto;
        padding: 1.25rem 1.5rem;
    }

    .legal-modal-loading {
        color: var(--text-muted);
        text-align: center;
        padding: 2rem 0;
    }

    /* === COOKIE MODAL === */
    .cookie-modal {
        position: fixed;
        inset: 0;
        z-index: 300;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
        opacity: 0;
        pointer-events: none;
        transition: opacity 300ms ease;
    }

    .cookie-modal.cookie-modal-visible {
        opacity: 1;
        pointer-events: all;
    }

    .cookie-backdrop {
        position: absolute;
        inset: 0;
        background: rgba(28, 25, 23, 0.6);
        backdrop-filter: blur(4px);
    }

    .cookie-card {
        position: relative;
        background: white;
        border-radius: var(--radius-2xl);
        box-shadow: var(--shadow-card-lifted);
        padding: 2rem;
        max-width: 26rem;
        width: 100%;
        text-align: center;
        transform: scale(0.9) translateY(16px);
        transition: transform 300ms ease;
    }

    .cookie-modal-visible .cookie-card {
        transform: scale(1) translateY(0);
    }

    .cookie-icon {
        width: 4rem;
        height: 4rem;
        border-radius: 50%;
        background: var(--amber-50);
        border: 2px solid var(--amber-200);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1rem;
        font-size: 1.75rem;
        color: var(--amber-600);
    }

    .cookie-title {
        font-size: 1.25rem;
        font-weight: 800;
        color: var(--text-primary);
        margin-bottom: 0.5rem;
    }

    .cookie-desc {
        font-size: 0.875rem;
        color: var(--text-secondary);
        line-height: 1.6;
        margin-bottom: 1.5rem;
    }

    .cookie-actions {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    @media (min-width: 480px) {
        .cookie-actions {
            flex-direction: row;
        }
    }

    .cookie-btn {
        flex: 1;
        padding: 0.75rem 1rem;
        min-height: 44px;
        border-radius: var(--radius-xl);
        font-size: 0.875rem;
        font-weight: 700;
        cursor: pointer;
        transition: all 200ms ease;
        border: none;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .cookie-btn:focus-visible {
        outline: 2px solid var(--border-focus);
        outline-offset: 2px;
    }

    .cookie-btn-secondary {
        background: var(--stone-100);
        color: var(--stone-600);
        border: 1px solid var(--border-default);
    }

    .cookie-btn-secondary:hover {
        background: var(--stone-200);
        color: var(--stone-800);
    }

    .cookie-btn-primary {
        background: linear-gradient(135deg, var(--orange-500) 0%, var(--orange-600) 100%);
        color: white;
        box-shadow: var(--shadow-btn);
    }

    .cookie-btn-primary:hover {
        transform: translateY(-1px);
        box-shadow: 0 4px 12px rgba(249, 115, 22, 0.4);
    }

    .cookie-link {
        display: block;
        margin-top: 1rem;
        font-size: 0.75rem;
        color: var(--text-muted);
        text-decoration: underline;
    }

    .cookie-link:hover {
        color: var(--amber-600);
    }
</style>
