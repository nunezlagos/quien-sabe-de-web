---
import SkeletonCard from './SkeletonCard.astro';

const { count = 6 } = Astro.props;
---

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6" aria-hidden="true" id="skeleton-grid">
  {Array.from({ length: count }, (_, i) => (
    <SkeletonCard />
  ))}
</div>
